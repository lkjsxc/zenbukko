# Transcription Outputs

## Outputs

- Extracted `.wav` when input is not already WAV.
- Transcript file matching requested `txt`, `srt`, or `vtt` format.

## Aggregates

- Chapter transcript aggregates are named `chapter-<chapterId>_transcription.md`.
- Chapter aggregates group lesson transcripts in lesson order.
